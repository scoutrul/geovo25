# 📦 Компоненты проекта

## Структура и организация

### 📁 base/ - Базовые компоненты

Атомарные, переиспользуемые компоненты без бизнес-логики.

#### BaseButton.vue
Универсальная кнопка с поддержкой вариаций.

```vue
<BaseButton 
  variant="primary"    <!-- primary | secondary | outline | ghost -->
  size="md"           <!-- sm | md | lg -->
  :loading="false"
  :disabled="false"
  @click="handleClick"
>
  Текст кнопки
</BaseButton>
```

**Варианты:**
- `primary` - основная зеленая кнопка
- `secondary` - вторичная темная кнопка  
- `outline` - кнопка с обводкой
- `ghost` - прозрачная кнопка

**Размеры:**
- `sm` - высота 40px, padding 16px
- `md` - высота 48px, padding 24px (по умолчанию)
- `lg` - высота 56px, padding 32px

**События:**
- `@click` - клик по кнопке

---

#### BaseContainer.vue
Адаптивный контейнер для контента.

```vue
<BaseContainer 
  max-width="full"    <!-- sm | md | lg | xl | full -->
  :padding="true"
>
  Контент
</BaseContainer>
```

**Максимальная ширина:**
- `sm` - 640px
- `md` - 768px
- `lg` - 1024px
- `xl` - 1280px
- `full` - 100% (по умолчанию)

**Padding:**
- `true` - адаптивные отступы (16px на мобильных, до 32px на десктопе)
- `false` - без отступов

---

#### BaseHeading.vue
Семантические заголовки с типографикой.

```vue
<BaseHeading 
  :level="4"          <!-- 1-6 -->
  as="h2"            <!-- h1, h2, h3, h4, h5, h6, div -->
  color="white-90"   <!-- цвет из палитры -->
>
  Заголовок
</BaseHeading>
```

**Уровни:**
- `1` - самый крупный заголовок
- `4` - 46px, используется в макете (по умолчанию)
- `6` - самый маленький заголовок

**Семантика:**
- `as` позволяет менять HTML тег независимо от стиля
- Пример: `level="4" as="h2"` - стиль h4, но семантика h2

---

#### BaseText.vue
Текстовые блоки с настройкой типографики.

```vue
<BaseText 
  as="p"              <!-- p, span, div -->
  size="p2"           <!-- p1 | p2 | p3 | body | body-sm -->
  color="white-100"   <!-- цвет из палитры -->
  weight="normal"     <!-- normal | medium | semibold | bold -->
>
  Текст
</BaseText>
```

**Размеры:**
- `p1` - 18px, line-height 1.6
- `p2` - 16px, line-height 1.6 (по умолчанию)
- `p3` - 15px, line-height 1.6, font-weight 500
- `body` - 16px
- `body-sm` - 14px

---

### 📁 ui/ - Составные UI компоненты

Компоненты, комбинирующие базовые элементы.

#### BenefitCard.vue
Карточка преимущества с текстом.

```vue
<BenefitCard 
  text="Запустим продукт за 1–3 месяца"
/>
```

**Props:**
- `text` (String, required) - текст преимущества

**Композиция:**
- Использует `BaseText` для отображения

---

### 📁 sections/ - Секции страницы

Крупные блоки страницы со сложной композицией.

#### BenefitsSection.vue
Секция "Что нас ждет впереди" с карточками и CTA.

```vue
<BenefitsSection
  title="Что нас ждет впереди"
  :benefits="benefitsArray"
  cta-text="Готовы перейти на новый уровень?"
  cta-button-text="Познакомиться"
  @cta-click="handleClick"
/>
```

**Props:**
- `title` (String) - заголовок секции
- `benefits` (Array) - массив объектов `{ text: string }`
- `ctaText` (String) - текст призыва к действию
- `ctaButtonText` (String) - текст на кнопке

**События:**
- `@cta-click` - клик по CTA кнопке

**Структура:**
```javascript
// Пример данных для benefits
const benefits = [
  { text: 'Запустим продукт за 1–3 месяца' },
  { text: 'Создадим с нуля интерфейсы' },
  // ...
]
```

**Композиция:**
- `BaseContainer` - адаптивный контейнер
- `BaseHeading` - заголовок секции
- `BenefitCard` - карточки преимуществ (grid 2 колонки)
- `BaseText` - текст CTA
- `BaseButton` - кнопка действия

**Layout:**
```
┌─────────────────────────────────────┐
│ Заголовок (H4)                      │
├─────────────────────────────────────┤
│ ┌─────────┐  ┌─────────┐            │
│ │ Card 1  │  │ Card 2  │            │
│ └─────────┘  └─────────┘            │
│ ┌─────────┐  ┌─────────┐            │
│ │ Card 3  │  │ Card 4  │            │
│ └─────────┘  └─────────┘            │
├─────────────────────────────────────┤
│ CTA текст                           │
│ [Кнопка]                            │
└─────────────────────────────────────┘
```

---

## 🎨 Стилизация

Все компоненты используют **Tailwind CSS utility классы** с кастомной конфигурацией из `tailwind.config.js`.

### Цветовая палитра

```javascript
primary: '#429512'      // Основной зеленый
bg-dark: '#161616'      // Темный фон секций
text-gray: '#5c5c5c'    // Серый вторичный текст
text-light: '#f6f6f5'   // Светлый текст
text-white: '#ffffff'   // Белый текст
```

### Типографика

```javascript
h4: 46px / 1.2 / 500    // Крупные заголовки
p1: 18px / 1.6 / 400    // Крупный текст
p2: 16px / 1.6 / 400    // Основной текст (по умолчанию)
p3: 15px / 1.6 / 500    // Вторичные заголовки
```

### Spacing

```javascript
gap-16 (64px)  - между основными блоками секции
gap-8 (32px)   - между карточками в grid
gap-6 (24px)   - внутри карточек
gap-2 (8px)    - между мелкими элементами
```

---

## 📱 Адаптивность

Все компоненты адаптивны и используют **mobile-first** подход.

**Breakpoints:**
- `< 640px` - мобильные (1 колонка)
- `640px - 1024px` - планшеты (2 колонки)
- `>= 1024px` - десктоп (можно расширить)

**Пример:**
```vue
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3">
  <!-- 1 колонка на мобильных -->
  <!-- 2 колонки на планшетах -->
  <!-- 3 колонки на десктопе -->
</div>
```

---

## 🔌 Импорт компонентов

### Через index.js (рекомендуется для base)

```vue
<script setup>
import { 
  BaseButton, 
  BaseContainer, 
  BaseHeading, 
  BaseText 
} from '@/components/base'
</script>
```

### Прямой импорт

```vue
<script setup>
import BenefitCard from '@/components/ui/BenefitCard.vue'
import BenefitsSection from '@/components/sections/BenefitsSection.vue'
</script>
```

---

## 🚀 Добавление новых компонентов

### 1. Определите категорию

- **base/** - атомарный, переиспользуемый без бизнес-логики?
- **ui/** - составной компонент с логикой?
- **sections/** - крупная секция страницы?

### 2. Создайте файл

```bash
src/components/base/BaseInput.vue
src/components/ui/SearchBar.vue
src/components/sections/HeroSection.vue
```

### 3. Следуйте паттерну

```vue
<template>
  <div :class="classes">
    <slot />
  </div>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  // Props с validation
  variant: {
    type: String,
    default: 'default',
    validator: (v) => ['default', 'custom'].includes(v)
  }
})

const emit = defineEmits(['event-name'])

const classes = computed(() => {
  // Computed для динамических классов
})
</script>
```

### 4. Добавьте в index.js (для base)

```javascript
export { default as BaseNewComponent } from './BaseNewComponent.vue'
```

---

## ✅ Чек-лист качества компонента

- [ ] Props имеют validation и default значения
- [ ] Используются computed для динамических значений
- [ ] События именованы в kebab-case
- [ ] Есть слот для кастомного контента (если нужно)
- [ ] Компонент адаптивен (mobile-first)
- [ ] Используются utility классы Tailwind
- [ ] Semantic HTML (правильные теги)
- [ ] Доступен (accessibility)
- [ ] Переиспользуем
- [ ] Документирован

---

## 📚 Полезные ссылки

- [Vue 3 Composition API](https://vuejs.org/guide/extras/composition-api-faq.html)
- [Script Setup](https://vuejs.org/api/sfc-script-setup.html)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Atomic Design](https://bradfrost.com/blog/post/atomic-web-design/)

