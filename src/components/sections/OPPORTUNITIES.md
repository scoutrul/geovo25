# OpportunitiesSection Component

## Описание
Секция "Какие возможности вы получите" с центральным логотипом, карточками возможностей и footer с кнопкой Figma. На desktop карточки позиционируются абсолютно вокруг логотипа, на mobile используется horizontal scroll.

## Структура
```
┌────────────────────────────────────────┐
│  Header: Заголовок + Подзаголовок      │
│                                        │
│  Flowchart:                            │
│    ┌──────┐      ┌──────┐             │
│    │ Card │      │ Card │             │
│    └──────┘  🟢  └──────┘             │
│              Logo                      │
│    ┌──────┐      ┌──────┐             │
│    │ Card │      │ Card │             │
│    └──────┘      └──────┘             │
│                                        │
│  Footer:                               │
│  ┌─────────────┬─────────────┐        │
│  │ 🎨 Figma    │ Текст       │        │
│  │ Текст       │             │        │
│  └─────────────┴─────────────┘        │
│  [Кнопка "Смотреть в Figma"]          │
└────────────────────────────────────────┘
```

## Свойства (Props)

| Свойство | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| title | String | Нет | 'Какие возможности вы получите' | Заголовок секции |
| subtitle | String | Нет | 'Для тех, кто любит подушнить' | Подзаголовок секции |
| logoUrl | String | Да | - | URL логотипа в центре |
| opportunities | Array | Да | - | Массив возможностей |
| footerText1 | String | Нет | Default text | Текст левой колонки footer |
| footerText2 | String | Нет | Default text | Текст правой колонки footer |
| buttonText | String | Нет | 'Смотреть в Figma' | Текст кнопки |

### Структура объекта opportunity
```javascript
{
  category: String,     // Название категории (обязательно)
  description: String   // Описание (обязательно, можно использовать \n)
}
```

## События (Emits)
- `figma-click` - вызывается при клике на кнопку "Смотреть в Figma"

## Адаптивность

### Mobile/Tablet (< 1024px)
- Заголовок: H4 (46px)
- Логотип: статичный, 160x160px в начале flowchart
- Карточки: horizontal scroll, 260px ширина
- Footer: одна колонка (flex-col)
- Padding flowchart: py-16 (64px)

### Desktop (≥ 1024px)
- Заголовок: H3 (64px)
- Логотип: центр flowchart, абсолютное позиционирование
- Карточки: абсолютное позиционирование, 320px ширина
- Footer: две колонки (flex-row)
- Высота flowchart: 782px (фиксированная)

## Дизайн

### Цвета
- Фон секции: `bg-white-90` (#f6f6f5)
- Заголовок: `text-black-90` (#161616)
- Подзаголовок: `text-black-50` (#5c5c5c)
- Логотип фон: `bg-primary` (#429512)
- Blur эффекты: `bg-primary` и `bg-secondary` (#e2ff83)

### Логотип
- Размер: 160x160px
- Фон: primary зеленый
- Border radius: 32px
- Blur эффекты: 2 круга 149x149px с blur-[60px]

### Footer
- Border top: `border-black-90/5`
- Gap между элементами: 32px
- Padding top: 64px (xl: 128px)

### Кнопка
- Вариант: `secondary` (с border)
- Размер: `md` (h-12)
- При клике открывает Figma

## Использование

### Базовый пример
```vue
<OpportunitiesSection
  :logo-url="logoUrl"
  :opportunities="opportunities"
  @figma-click="handleFigmaClick"
/>
```

### Полный пример
```vue
<script setup>
const opportunities = [
  {
    category: 'Задачи',
    description: 'Приоритизация и планирование в JIRA\nСпринты, сроки, оценка и контроль скорости\nДейли, ретро, демо, 1×1 — всё по канону продуктовых циклов'
  },
  {
    category: 'Разработка',
    description: 'Контроль pixel-perfect реализации\nТестирование сценариев, поиск ошибок\nПроверка верстки на pixel perfect'
  },
  {
    category: 'Аналитика',
    description: 'Анализ рынка и конкурентной среды\nОформление гипотез\nМониторинг активности'
  }
  // ... еще 3 карточки для оптимального отображения
]

const logoUrl = 'https://example.com/logo.svg'

const handleFigmaClick = () => {
  window.open('https://figma.com/file/...', '_blank')
}
</script>

<template>
  <OpportunitiesSection
    title="Какие возможности вы получите"
    subtitle="Для тех, кто любит подушнить"
    :logo-url="logoUrl"
    :opportunities="opportunities"
    footer-text1="Для тех, кто хочет копнуть глубже —\nя открыл доступ к Figma-файлу с дизайном этого сайта"
    footer-text2="Там можно посмотреть структуру проекта, токены, компоненты, сетки и прикрепленные комментарии"
    button-text="Смотреть в Figma"
    @figma-click="handleFigmaClick"
  />
</template>
```

## Позиционирование карточек на Desktop

Карточки позиционируются абсолютно относительно flowchart контейнера (1592px ширина):

1. **left: 289px, top: 70px** - верхняя левая
2. **left: 151px, top: 349px** - средняя левая
3. **left: 878px, top: 0** - верхняя правая
4. **left: 1083px, top: 246px** - средняя правая
5. **left: 878px, top: 532px** - нижняя правая
6. **left: 608px, top: 628px** - нижняя центральная

**Логотип**: центрируется автоматически через `left-1/2 top-1/2 -translate-x/y-1/2`

## Компоненты

### Используемые базовые компоненты
- `BaseContainer` - контейнер секции
- `BaseHeading` - заголовки (H3/H4)
- `BaseText` - текст подзаголовка и footer
- `BaseButton` - кнопка Figma (secondary variant)

### Используемые UI компоненты
- `OpportunityCard` - карточка возможности

## Макет Figma
Компонент создан на основе: 
https://www.figma.com/design/IYufH6FFbWEplvqjKOB6Gf/geovo?node-id=4265-2404

## Валидация

Компонент включает валидацию массива `opportunities`:
- Каждый элемент должен содержать `category` (String)
- Каждый элемент должен содержать `description` (String)

## Рекомендации

### Количество карточек
- **Оптимально**: 6 карточек (соответствует макету)
- **Минимум**: 3 карточки
- **Максимум**: 6 карточек для сохранения дизайна

При другом количестве карточек может потребоваться корректировка позиций.

### Логотип
- Формат: SVG или PNG с прозрачностью
- Рекомендуемый размер: 100-120px (внутри контейнера 160px)
- Цвет: белый или светлый (на зеленом фоне)

### Контент
- Категории: 1-2 слова
- Описания: 2-4 строки, ~100-200 символов
- Footer текст: информативный, приглашающий

### Footer текст
Используйте `\n` для переноса строк:
```javascript
footerText1: 'Строка 1\nСтрока 2'
```

## Особенности реализации

### Адаптивное позиционирование
```vue
<!-- Desktop: absolute -->
<OpportunityCard
  v-if="gtLg"
  class="absolute left-[289px] top-[70px] w-[320px]"
  v-bind="opportunity"
/>

<!-- Mobile: horizontal scroll -->
<div v-else class="flex overflow-x-auto">
  <OpportunityCard
    class="min-w-[260px]"
    v-bind="opportunity"
  />
</div>
```

### Blur эффекты логотипа
Создают визуальное свечение вокруг логотипа:
```vue
<div class="bg-primary blur-[60px]" />
<div class="bg-secondary blur-[60px]" />
```

### Figma иконка
SVG встроен напрямую в компонент для оптимизации загрузки.

## Производительность

- Логотип загружается асинхронно через img
- Blur эффекты используют CSS filter
- Абсолютное позиционирование не вызывает reflow

## Связанные компоненты
- `OpportunityCard` - карточка возможности
- `BaseButton` - кнопка с secondary вариантом
- `HowWeWorkSection` - похожая структура с иконками

## История изменений
- v1.0.0 (2024) - Первоначальная реализация на основе Figma макета

