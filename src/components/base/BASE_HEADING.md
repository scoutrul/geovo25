# BaseHeading

Универсальный компонент заголовков с **автоматической фиксацией высоты** для каждого экземпляра. Предотвращает "прыжки" layout при анимациях и динамической загрузке контента.

## 🎯 Особенности

- ✅ **Полностью инкапсулирован** - каждый экземпляр компонента имеет свои собственные измерения
- ✅ **Автоматическое измерение** - высота фиксируется сразу после монтирования
- ✅ **Адаптивность** - автоматический пересчет при смене брейкпоинтов
- ✅ **Нет CLS** - предотвращает Cumulative Layout Shift
- ✅ **Работает из коробки** - не требует дополнительных props или настроек

## Props

### level
- **Тип:** `Number | String`
- **По умолчанию:** `2`
- **Допустимые значения:** `1, 2, 3, 4, 5, 6`
- Уровень заголовка (h1-h6)

### as
- **Тип:** `String`
- **По умолчанию:** `null`
- Переопределяет HTML-тег (например, можно сделать `h1` который выглядит как `h3`)

### size
- **Тип:** `String`
- **По умолчанию:** `null`
- **Допустимые значения:** `'h5', 'p1', 'p2', 'p3'`
- Переопределяет размер текста

## Примеры использования

### 1. Базовое использование

Высота автоматически фиксируется для каждого экземпляра:

```vue
<BaseHeading :level="1">
  Заголовок с автофиксацией высоты
</BaseHeading>
```

### 2. С анимацией ScrambleText

Идеально работает с текстовыми анимациями:

```vue
<BaseHeading :level="1" class="text-white-100">
  <span ref="titleRef" class="block"></span>
  <span ref="subtitleRef" class="block"></span>
</BaseHeading>
```

### 3. Реальный пример из HeroSection

```vue
<BaseHeading
  :level="gtXl ? 3 : 5"
  :as="'h1'"
  class="text-white-100"
>
  <span ref="titleRef" class="block"></span>
  <span ref="subtitleRef" class="block"></span>
</BaseHeading>
```

### 4. Переопределение тега и размера

```vue
<!-- h1 который выглядит как h3 -->
<BaseHeading :level="3" :as="'h1'">
  SEO-friendly заголовок
</BaseHeading>

<!-- С кастомным размером -->
<BaseHeading :level="2" size="p1">
  Заголовок размером как параграф
</BaseHeading>
```

### 5. Множество экземпляров

Каждый экземпляр независимо измеряет свою высоту:

```vue
<BaseHeading :level="1">Первый заголовок</BaseHeading>
<BaseHeading :level="2">Второй заголовок</BaseHeading>
<BaseHeading :level="3">Третий заголовок</BaseHeading>
<!-- Каждый имеет свою собственную зафиксированную высоту -->
```

## Как это работает

### Инкапсуляция на уровне экземпляра

Каждый экземпляр `BaseHeading` имеет:
- **Собственный ref** (`headingRef`) - уникальная ссылка на DOM элемент
- **Собственное состояние** (`measuredHeight`) - измеренная высота конкретно этого элемента
- **Собственные lifecycle hooks** - независимые `onMounted` и `watch`

```javascript
// Внутри каждого экземпляра компонента:
const headingRef = ref(null);         // Уникален для этого экземпляра
const measuredHeight = ref(null);     // Уникален для этого экземпляра
```

### Процесс измерения

1. **При монтировании компонента** (`onMounted`):
   - Компонент рендерится с контентом
   - Измеряется реальная высота элемента через `offsetHeight`
   - Высота фиксируется в `measuredHeight`
   - Применяется `min-height` в стилях

2. **При изменении брейкпоинта** (`watch(currentBreakpoint)`):
   - Высота временно сбрасывается (`measuredHeight.value = null`)
   - Ожидается следующий тик (`nextTick`)
   - Повторно измеряется новая высота
   - Применяется новый `min-height`

3. **Предотвращение "прыжков" layout**:
   - После фиксации, элемент сохраняет стабильную высоту
   - Анимации контента не влияют на окружающие элементы
   - Нет Cumulative Layout Shift (CLS)

## Брейкпоинты

Компонент автоматически реагирует на изменение брейкпоинтов:

| Название | Размер экрана | Устройство |
|----------|---------------|------------|
| `xs`     | < 640px       | Mobile     |
| `sm`     | >= 640px      | Tablet portrait |
| `md`     | >= 768px      | Tablet landscape |
| `lg`     | >= 1024px     | Desktop    |
| `xl`     | >= 1280px     | Large desktop |
| `2xl`    | >= 1536px     | Extra large desktop |

## Преимущества автоматической фиксации

✅ **Помогает когда:**
- Есть анимации текста (ScrambleText, печатающий эффект и т.д.)
- Контент загружается динамически после монтирования
- Нужна стабильная высота для предотвращения CLS (Cumulative Layout Shift)
- Используется в CSS Grid/Flexbox layout
- Важна производительность и метрики Core Web Vitals

⚠️ **Особенности:**
- Высота фиксируется автоматически для всех заголовков
- Если контент может значительно меняться динамически, учитывайте фиксированную высоту
- Пересчет происходит только при смене брейкпоинтов, а не при любом resize

## Производительность

- **Легковесность**: Измерение происходит только при монтировании и смене брейкпоинтов
- **Оптимизация**: Используется `nextTick()` для корректного измерения после рендера
- **Реактивность**: Благодаря `useBreakpoints` и VueUse, отслеживание resize оптимизировано

