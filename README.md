# Goevo25 - Vue 3 + Tailwind CSS Project

Проект с современной архитектурой Vue 3, использующий Composition API и Tailwind CSS для создания адаптивных интерфейсов.

## 🚀 Технологический стек

- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vite** - быстрая сборка и hot reload
- **Tailwind CSS** - utility-first CSS фреймворк
- **GSAP** - профессиональные анимации
- **VueUse** - коллекция утилит и composables
- **Composition API** - современный подход к логике компонентов

## 📁 Структура проекта

```
src/
├── assets/
│   └── styles/
│       └── main.css              # Основные стили, Tailwind конфигурация
│
├── components/
│   ├── base/                     # Базовые атомарные компоненты
│   │   ├── BaseButton.vue        # Кнопка с вариациями (primary, secondary, outline, ghost)
│   │   ├── BaseContainer.vue     # Адаптивный контейнер с настройками ширины
│   │   ├── BaseHeading.vue       # Заголовки с semantic HTML
│   │   ├── BaseText.vue          # Текстовые блоки с типографикой
│   │   └── index.js              # Экспорт базовых компонентов
│   │
│   ├── ui/                       # Составные UI компоненты
│   │   ├── BenefitCard.vue       # Карточка преимущества
│   │   └── AccordionItem.vue     # Элемент аккордиона FAQ
│   │
│   └── sections/                 # Секции страницы
│       ├── FaqSection.vue        # FAQ секция с аккордионом
│       └── BenefitsSection.vue   # Секция "Что нас ждет впереди"
│
├── composables/                  # Переиспользуемая бизнес-логика
│   ├── useBreakpoints.js         # Работа с адаптивностью
│   └── useAccordion.js           # Логика аккордиона
│
├── App.vue                       # Корневой компонент
└── main.js                       # Точка входа приложения
```

## 🎨 Дизайн-система

> Полная документация в файле `DESIGN_SYSTEM.md`

### Цветовая палитра

```javascript
primary:        '#429512'  // Основной зеленый
primary-hover:  '#4fa61d'  // Hover состояние
primary-active: '#3b870e'  // Active состояние

black-90: '#161616'  // Темный фон секций
black-50: '#5c5c5c'  // Серый вторичный текст ✅

white-100: '#ffffff'  // Чисто белый
white-90:  '#f6f6f5'  // Светлый фон/текст
white-80:  '#dedede'  // Средний светлый
```

### Типографика (Шрифт: Neue Montreal)

- **H1**: 180px / 0.9 / Medium
- **H2**: 100px / 0.95 / Medium
- **H3**: 64px / 1.0 / Medium
- **H4**: 46px / 1.0 / Medium (используется в макете)
- **H5**: 28px / 1.3 / Medium
- **P1**: 20px / 1.4 / Regular
- **P2**: 16px / 1.6 / Regular (основной текст)
- **P3**: 14px / 1.8 / Regular

### Spacing

- gap-64px (16rem) - между основными блоками
- gap-32px (8rem) - между карточками
- gap-24px (6rem) - внутри карточек
- gap-8px (2rem) - между мелкими элементами

## 🧩 Архитектура компонентов

### BaseButton

Универсальная кнопка согласно дизайн-системе Figma с полной поддержкой всех состояний.

**Props:**
- `variant`: 'primary' | 'secondary' | 'outline' | 'ghost'
- `size`: 'sm' | 'md' | 'lg'
- `disabled`: boolean
- `loading`: boolean
- `type`: 'button' | 'submit' | 'reset'

**Варианты (из Figma):**
- **Primary:** Зеленая кнопка для главных действий
- **Secondary:** Кнопка с обводкой для вторичных действий
- **Outline:** Обводка primary для альтернативных действий
- **Ghost:** Прозрачная для третичных действий

**Состояния (автоматические):**
- Base → Hover → Active
- Disabled (opacity-50)
- Loading (spinner + disabled)

**Пример использования:**
```vue
<!-- Primary кнопка -->
<BaseButton variant="primary" size="md" @click="handleClick">
  Познакомиться
</BaseButton>

<!-- Secondary кнопка -->
<BaseButton variant="secondary" size="md">
  Подробнее
</BaseButton>

<!-- Loading состояние -->
<BaseButton variant="primary" :loading="isLoading">
  Сохранить
</BaseButton>
```

> Полная документация: `BUTTONS.md`

### BaseContainer

Универсальный адаптивный контейнер с поддержкой фона, отступов и семантических тегов.

**Props:**
- `maxWidth`: 'sm' | 'md' | 'lg' | 'xl' | 'full' - максимальная ширина контента
- `padding`: boolean - горизонтальные отступы (px-4 sm:px-6 lg:px-8)
- `verticalPadding`: boolean | 'sm' | 'md' | 'lg' | 'xl' - вертикальные отступы (адаптивные)
- `bg`: string - класс фона Tailwind (например, 'bg-white-90', 'bg-black-90')
- `as`: string - HTML тег для рендера (по умолчанию 'div')

**Примеры:**
```vue
<!-- Секция с фоном и отступами -->
<BaseContainer 
  :as="'section'"
  :vertical-padding="true"
  :bg="'bg-white-90'"
>
  Content
</BaseContainer>

<!-- Контейнер без вертикальных отступов -->
<BaseContainer :padding="true" :max-width="'xl'">
  Content
</BaseContainer>
```

> Полная документация: `BASE_CONTAINER.md`

### BaseHeading

Семантические заголовки с типографикой из дизайн-системы.

**Props:**
- `level`: 1-6 (уровень заголовка)
- `as`: string (HTML тег для рендера)
- `color`: string (цвет из палитры)

### BaseText

Текстовые блоки с настройкой размера и веса.

**Props:**
- `as`: string (HTML тег)
- `size`: 'p1' | 'p2' | 'p3' | 'body' | 'body-sm'
- `color`: string
- `weight`: 'normal' | 'medium' | 'semibold' | 'bold'

## 🔧 Composables

### useBreakpoints

Отслеживает размер окна и предоставляет реактивные флаги для адаптивности. Использует **VueUse** для оптимизированного отслеживания.

**Основные возможности:**
```javascript
const {
  width, height,           // Размеры окна (реактивные)
  isMobile, isTablet,      // Основные флаги
  isDesktop,               // >= 1024px
  
  // Детальные флаги (Tailwind breakpoints)
  isXs, isSm, isMd, isLg, isXl, is2xl,
  
  // Greater/Less than
  gtSm, gtMd, gtLg,        // >= breakpoint
  ltSm, ltMd, ltLg,        // < breakpoint
  
  // Ориентация
  isPortrait, isLandscape,
  
  // Мета-данные
  deviceType,              // 'mobile' | 'tablet' | 'desktop'
  currentBreakpoint        // 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'
} = useBreakpoints()
```

### useAccordion

Управляет состоянием аккордиона (открыто/закрыто) с поддержкой автоматического открытия на десктопе.

**Параметры:**
- `itemCount`: количество элементов
- `defaultOpenOnDesktop`: автоматически открыть все на desktop

**Возвращает:**
```javascript
{
  openItems,      // Set с индексами открытых элементов
  toggleItem,     // Переключить элемент
  isItemOpen,     // Проверить открыт ли элемент
  openAll,        // Открыть все
  closeAll        // Закрыть все
}
```

## 🎬 Анимации

Проект использует **GSAP 3.13.0** для плавных профессиональных анимаций.

**Где используется:**
- AccordionItem - плавное открытие/закрытие FAQ
- Easing: `power2.out` (открытие), `power2.in` (закрытие)
- Duration: 400ms (открытие), 300ms (закрытие)

> Полная документация: `GSAP_ANIMATIONS.md`

## 🚀 Команды

```bash
# Установка зависимостей
pnpm install

# Запуск dev сервера
pnpm dev

# Сборка для production
pnpm build

# Предпросмотр production сборки
pnpm preview
```

## 📱 Адаптивность

Проект использует mobile-first подход:
- Mobile: < 640px (1 колонка)
- Tablet: 640px - 1024px (2 колонки)
- Desktop: >= 1024px (можно расширить до 3-4 колонок)

## 🎯 Best Practices

1. **Декомпозиция**: Компоненты разбиты на атомарные (base), составные (ui) и секции
2. **Composition API**: Логика вынесена в composables для переиспользования
3. **Props validation**: Все props с валидацией типов
4. **Semantic HTML**: Правильное использование тегов для доступности
5. **Tailwind CSS**: Использование utility классов с кастомной палитрой

## 📚 Документация

- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура проекта
- [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md) - Дизайн-система
- [BASE_CONTAINER.md](./BASE_CONTAINER.md) - Универсальный контейнер
- [BUTTONS.md](./BUTTONS.md) - Документация по кнопкам
- [QUICK_START_BUTTONS.md](./QUICK_START_BUTTONS.md) - Быстрый старт с кнопками
- [FAQ_ACCORDION.md](./FAQ_ACCORDION.md) - FAQ секция и аккордион
- [GSAP_ANIMATIONS.md](./GSAP_ANIMATIONS.md) - GSAP анимации
- [VUEUSE_INTEGRATION.md](./VUEUSE_INTEGRATION.md) - VueUse интеграция
- [PROJECT_OVERVIEW.md](./PROJECT_OVERVIEW.md) - Общий обзор проекта

## 🔮 Дальнейшее развитие

- [ ] Подключить шрифт TT Neoris Trial
- [x] Добавить анимации с GSAP
- [x] Интегрировать VueUse для улучшенной реактивности
- [ ] Расширить палитру компонентов
- [ ] Добавить unit тесты
- [ ] Настроить ESLint и Prettier

