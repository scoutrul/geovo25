# 📋 FAQ Секция с Аккордионом

## 📊 Обзор

FAQ секция с аккордионом, который автоматически раскрывается на десктопе (>= 1024px) и закрыт на мобильных/планшетах.

---

## 🎯 Особенности

✅ **Адаптивное поведение:**
- Desktop (>= 1024px): все вопросы раскрыты по умолчанию
- Tablet/Mobile (< 1024px): все вопросы закрыты, открываются по клику
- Использует `useBreakpoints` с VueUse для отслеживания

✅ **Плавные анимации GSAP:**
- Открытие: 400ms с ease 'power2.out'
- Закрытие: 300ms с ease 'power2.in'
- Профессиональная анимация height + opacity через GSAP
- Без hover эффекта на заголовках

✅ **Автоматический пересчет высоты:**
- При ресайзе окна (через `watch(width)`)
- При изменении контента (через `useResizeObserver` из VueUse)
- Плавная GSAP анимация до новой высоты (200ms)

✅ **Дизайн из Figma:**
- Светлый фон: `bg-white-90 (#f6f6f5)`
- Border между элементами: `border-white-80 (#dedede)`
- Текст вопроса: `text-black-90 (#161616)`
- Текст ответа: `text-black-10 (#303030)`

✅ **Типографика:**
- Заголовок секции: `H3` (64px на desktop, 46px на mobile)
- Вопросы: `H5` (28px на desktop), `P1` (20px на mobile)
- Ответы: `P2` (16px)

---

## 📁 Структура компонентов

```
components/
├── sections/
│   └── FaqSection.vue        # Секция с FAQ
├── ui/
│   └── AccordionItem.vue     # Элемент аккордиона
└── composables/
    └── useAccordion.js       # Логика аккордиона
```

---

## 🚀 Использование

### Базовый пример

```vue
<template>
  <FaqSection
    :title="'Частые вопросы'"
    :faq-items="faqData"
  />
</template>

<script setup>
import { reactive } from 'vue'
import FaqSection from '@/components/sections/FaqSection.vue'

const faqData = reactive([
  {
    question: 'Вопрос 1?',
    answer: 'Ответ на вопрос 1'
  },
  {
    question: 'Вопрос 2?',
    answer: 'Ответ на вопрос 2 с переносами.\n\nВторой параграф'
  }
])
</script>
```

### С кастомным заголовком

```vue
<FaqSection
  :title="'Вопросы и ответы'"
  :faq-items="faqData"
/>
```

---

## 🧩 Компоненты

### FaqSection.vue

**Props:**
- `title` (String) - заголовок секции (по умолчанию: "Частые вопросы")
- `faqItems` (Array, required) - массив вопросов и ответов

**Структура faqItems:**
```javascript
[
  {
    question: String,  // Текст вопроса
    answer: String     // Текст ответа (поддерживает \n для переносов)
  }
]
```

---

### AccordionItem.vue

**Props:**
- `question` (String, required) - текст вопроса
- `answer` (String, required) - текст ответа
- `isOpen` (Boolean) - открыт ли элемент
- `isLast` (Boolean) - последний ли элемент (убирает нижний border)

**Events:**
- `@toggle` - эмитится при клике на вопрос

**Пример:**
```vue
<AccordionItem
  :question="'Вопрос'"
  :answer="'Ответ'"
  :is-open="true"
  :is-last="false"
  @toggle="handleToggle"
/>
```

---

## 🔧 Composable: useAccordion

**Параметры:**
- `itemsCount` (Number) - количество элементов аккордиона

**Возвращает:**
```javascript
{
  openItems,    // Set открытых индексов
  isOpen,       // (index) => Boolean - проверка открыт ли элемент
  toggle,       // (index) => void - переключение элемента
  open,         // (index) => void - открыть элемент
  close,        // (index) => void - закрыть элемент
  openAll,      // () => void - открыть все
  closeAll,     // () => void - закрыть все
  isDesktop     // Ref<Boolean> - флаг десктопа
}
```

**Пример использования:**
```vue
<script setup>
import { useAccordion } from '@/composables/useAccordion'

const items = [...]
const { isOpen, toggle, openAll, closeAll } = useAccordion(items.length)

// Открыть конкретный элемент
const handleClick = (index) => {
  toggle(index)
}

// Открыть все элементы
const expandAll = () => {
  openAll()
}
</script>
```

---

## 🎨 Стилизация

### Цвета
```css
bg-white-90:    #f6f6f5  /* Фон секции */
border-white-80: #dedede  /* Border между элементами */
text-black-90:  #161616  /* Текст вопроса */
text-black-10:  #303030  /* Текст ответа */
```

### Размеры и отступы
```css
/* Desktop */
px-[336px] py-[160px]  /* Padding секции */
px-8 py-4              /* Padding элемента */
gap-16 lg:gap-32       /* Gap между заголовком и списком */

/* Tablet */
px-[40px] py-[128px]   /* Padding секции */

/* Mobile */
px-[16px] py-[64px]    /* Padding секции */
```

### Анимация (GSAP)
```javascript
// Открытие - сначала показываем, потом анимируем
gsap.set(element, { display: 'block' })
gsap.to(element, {
  height: contentHeight,
  opacity: 1,
  duration: 0.4,
  ease: 'power2.out'
})

// Закрытие - сначала анимируем, потом скрываем
gsap.to(element, {
  height: 0,
  opacity: 0,
  duration: 0.3,
  ease: 'power2.in',
  onComplete: () => {
    gsap.set(element, { display: 'none' })
  }
})
```

**Важно:** 
- ❌ Не используем `v-show` - он прервет анимацию закрытия
- ✅ GSAP полностью управляет видимостью через `display`
- ✅ `display: 'none'` устанавливается ПОСЛЕ завершения анимации

---

## 📱 Адаптивность

### Desktop (>= 1024px)
- Все вопросы раскрыты по умолчанию
- Заголовок: H3 (64px)
- Вопросы: H5 (28px)
- Padding элемента: 32px

### Tablet (640px - 1024px)
- Все вопросы закрыты
- Заголовок: H3 (64px)
- Вопросы: H5 (28px)

### Mobile (< 640px)
- Все вопросы закрыты
- Заголовок: H4 (46px)
- Вопросы: P1 (20px)
- Без padding элемента

---

## 💡 Примеры

### С многострочными ответами

```javascript
const faqData = [
  {
    question: 'Длинный вопрос?',
    answer: 'Первый параграф ответа.\n\nВторой параграф ответа.'
  }
]
```

### С HTML в ответах

```javascript
const faqData = [
  {
    question: 'Вопрос?',
    answer: 'Ответ с <strong>жирным текстом</strong>'
  }
]
```

---

## 🔄 Управление состоянием

### Программное управление

```vue
<script setup>
import { ref } from 'vue'
import { useAccordion } from '@/composables/useAccordion'

const items = [/* ... */]
const { isOpen, toggle, openAll, closeAll } = useAccordion(items.length)

// Открыть все при загрузке
onMounted(() => {
  openAll()
})

// Закрыть все
const collapseAll = () => {
  closeAll()
}

// Открыть только первый
const openFirst = () => {
  closeAll()
  open(0)
}
</script>
```

---

## ✅ Лучшие практики

### DO (Правильно)
✅ Используйте понятные вопросы  
✅ Держите ответы лаконичными  
✅ Используйте `\n\n` для разделения параграфов  
✅ Группируйте вопросы по темам  
✅ 6-10 вопросов оптимально  

### DON'T (Неправильно)
❌ Не делайте слишком длинные ответы  
❌ Не дублируйте информацию из других секций  
❌ Не используйте сложные HTML структуры в ответах  
❌ Не делайте больше 15 вопросов  

---

## 🚧 Дальнейшее развитие

- [ ] Добавить иконки +/- для раскрытия
- [ ] Добавить поиск по вопросам
- [ ] Добавить категории FAQ
- [ ] Добавить счетчик "полезно/не полезно"
- [ ] Добавить анимацию иконок
- [ ] Добавить deep linking (якоря к вопросам)

---

## 🎯 Производительность

- Легкие transitions (height + opacity)
- Minimal re-renders (Set для openItems)
- Эффективное управление состоянием
- Нет лишних вычислений

---

✅ **FAQ секция с аккордионом готова к использованию!**

