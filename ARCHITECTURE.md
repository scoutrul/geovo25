# 🏗️ Архитектура проекта Goevo25

## Принципы проектирования

### 1. **Атомарный дизайн (Atomic Design)**

```
Atoms (Атомы)         → base/BaseButton, BaseText, BaseHeading
Molecules (Молекулы)  → ui/BenefitCard
Organisms (Организмы) → sections/BenefitsSection
Templates             → (будущие layouts)
Pages                 → App.vue
```

### 2. **Separation of Concerns (Разделение ответственности)**

- **Компоненты** отвечают только за представление (UI)
- **Composables** содержат бизнес-логику и side-effects
- **Конфигурация** (Tailwind, Vite) вынесена в отдельные файлы

### 3. **Reusability (Переиспользование)**

Базовые компоненты максимально абстрактны и настраиваются через props:

```vue
<!-- Гибкость через props -->
<BaseButton variant="primary" size="md" :loading="isLoading" />
<BaseButton variant="outline" size="sm" :disabled="true" />
```

## 📊 Диаграмма зависимостей

```
App.vue
  └── BenefitsSection.vue
        ├── BaseContainer
        ├── BaseHeading
        ├── BaseText
        ├── BaseButton
        └── BenefitCard
              └── BaseText
```

## 🧱 Структура компонентов

### Base Components (Базовые)

**Характеристики:**
- Не зависят от других компонентов проекта
- Максимально переиспользуемы
- Настраиваются через props
- Минимальная логика (только UI)

**Примеры:**
- `BaseButton` - универсальная кнопка
- `BaseText` - типографика
- `BaseHeading` - заголовки
- `BaseContainer` - контейнеры

### UI Components (Составные)

**Характеристики:**
- Комбинируют базовые компоненты
- Имеют специфичную бизнес-логику
- Могут использовать composables

**Примеры:**
- `BenefitCard` - карточка преимущества

### Sections (Секции)

**Характеристики:**
- Крупные блоки страницы
- Управляют данными и состоянием
- Эмитят события наверх

**Примеры:**
- `BenefitsSection` - секция преимуществ

## 🔄 Паттерны коммуникации

### Props Down, Events Up

```vue
<!-- Родитель передает данные вниз -->
<BenefitsSection 
  :benefits="benefits" 
  @cta-click="handleClick" 
/>

<!-- Дочерний компонент эмитит события наверх -->
const emit = defineEmits(['cta-click'])
emit('cta-click')
```

### Composables для логики

```javascript
// Вместо миксинов - composables
const { isMobile, isDesktop } = useBreakpoints()
```

## 🎨 Стилизация

### Tailwind Utility-First Approach

**Преимущества:**
1. Нет конфликтов имен классов
2. Автоматическая оптимизация (PurgeCSS)
3. Консистентность через дизайн-токены
4. Быстрая разработка

**Пример:**
```vue
<div class="flex flex-col gap-16 bg-bg-dark p-4">
  <!-- Все стили - utility классы -->
</div>
```

### Кастомная конфигурация

```javascript
// tailwind.config.js
theme: {
  extend: {
    colors: {
      primary: '#429512',      // Кастомные цвета
    },
    fontSize: {
      'h4': ['46px', { ... }], // Типографика из макета
    }
  }
}
```

## 📦 Управление состоянием

**Текущий подход:**
- Локальное состояние через `reactive()` и `ref()`
- Props для передачи данных вниз
- Events для коммуникации наверх

**Будущее масштабирование:**
- Pinia для глобального состояния
- Composables для shared state

## 🔌 Composables - переиспользуемая логика

### Структура composable:

```javascript
export function useFeature() {
  // 1. Реактивное состояние
  const state = ref(null)
  
  // 2. Вычисляемые свойства
  const computed = computed(() => state.value * 2)
  
  // 3. Методы
  const method = () => { /* ... */ }
  
  // 4. Lifecycle hooks
  onMounted(() => { /* ... */ })
  
  // 5. Возврат публичного API
  return { state, computed, method }
}
```

### Примеры использования:

```vue
<script setup>
import { useBreakpoints } from '@/composables/useBreakpoints'

const { isMobile, isDesktop } = useBreakpoints()
</script>
```

## 🧪 Тестирование (будущее)

```
tests/
├── unit/
│   ├── components/
│   │   ├── BaseButton.spec.js
│   │   └── BenefitCard.spec.js
│   └── composables/
│       └── useBreakpoints.spec.js
└── e2e/
    └── benefits-section.spec.js
```

## 📈 Метрики качества

### Code Quality
- ✅ Декомпозиция компонентов
- ✅ Переиспользование через props
- ✅ Разделение логики (composables)
- ✅ TypeScript-ready структура
- ✅ Semantic HTML

### Performance
- ✅ Tree-shaking (Vite)
- ✅ CSS purging (Tailwind)
- ✅ Lazy loading (готово к внедрению)
- ✅ Оптимизация bundle size

### Maintainability
- ✅ Понятная структура папок
- ✅ Консистентный naming
- ✅ Документация
- ✅ Self-documenting код

## 🚀 Масштабирование

### Горизонтальное (больше функций)

```
components/
├── base/       # +10 новых компонентов
├── ui/         # +20 составных компонентов
├── sections/   # +15 секций
└── layouts/    # Новая категория
```

### Вертикальное (усложнение)

```javascript
// Добавление TypeScript
<script setup lang="ts">
interface Props {
  variant: 'primary' | 'secondary'
}
const props = defineProps<Props>()
</script>

// Добавление i18n
import { useI18n } from 'vue-i18n'
const { t } = useI18n()

// Добавление роутинга
import { useRouter } from 'vue-router'
const router = useRouter()
```

## 🎯 Best Practices реализованные в проекте

1. **Composition API** вместо Options API
2. **Script Setup** для лаконичности
3. **Props validation** для надежности
4. **Events naming** с кебаб-кейсом
5. **Composables** для логики
6. **Atomic Design** для структуры
7. **Mobile-first** подход
8. **Utility-first** CSS
9. **Semantic HTML** для доступности
10. **Self-documenting** код

## 📚 Источники вдохновения

- [Vue 3 Style Guide](https://vuejs.org/style-guide/)
- [Atomic Design](https://bradfrost.com/blog/post/atomic-web-design/)
- [Tailwind Best Practices](https://tailwindcss.com/docs/reusing-styles)

