# 📦 BaseContainer - Универсальный контейнер

## 📊 Обзор

`BaseContainer` — это универсальный адаптивный контейнер для всех секций приложения. Он инкапсулирует логику отступов, фона, максимальной ширины и семантической разметки.

---

## ✨ Особенности

✅ **Адаптивные отступы:**
- Горизонтальные: `px-4 sm:px-6 lg:px-8`
- Вертикальные: 4 уровня (sm, md, lg, xl)

✅ **Фон:**
- Любой Tailwind класс `bg-*`
- Централизованное управление цветами

✅ **Семантическая разметка:**
- Поддержка любого HTML тега через `as` prop
- Можно использовать как `<section>`, `<div>`, `<article>` и т.д.

✅ **Максимальная ширина:**
- 5 предустановленных размеров
- Автоматическое центрирование

---

## 📋 Props

| Prop | Type | Default | Описание |
|------|------|---------|----------|
| `maxWidth` | `'sm' \| 'md' \| 'lg' \| 'xl' \| 'full'` | `'full'` | Максимальная ширина контента |
| `padding` | `boolean` | `true` | Горизонтальные отступы |
| `verticalPadding` | `boolean \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `false` | Вертикальные отступы (адаптивные) |
| `bg` | `string` | `''` | Класс фона Tailwind (`bg-*`) |
| `as` | `string` | `'div'` | HTML тег для рендера |

---

## 🎨 Vertical Padding размеры

### `verticalPadding="sm"`
```
Mobile:  py-8   (32px)
Desktop: py-16  (64px)
XL:      py-20  (80px)
```

### `verticalPadding="md"`
```
Mobile:  py-12  (48px)
Desktop: py-24  (96px)
XL:      py-32  (128px)
```

### `verticalPadding="lg"` или `verticalPadding={true}`
```
Mobile:  py-16  (64px)
Desktop: py-32  (128px)
XL:      py-40  (160px)
```

### `verticalPadding="xl"`
```
Mobile:  py-20  (80px)
Desktop: py-40  (160px)
XL:      py-48  (192px)
```

---

## 💡 Примеры использования

### 1. Секция с фоном (FAQ)

```vue
<BaseContainer 
  :as="'section'"
  :padding="true" 
  :vertical-padding="true"
  :max-width="'full'"
  :bg="'bg-white-90'"
>
  <div class="max-w-[1048px] mx-auto">
    <!-- Контент FAQ -->
  </div>
</BaseContainer>
```

**Результат:**
- Рендерится как `<section>`
- Фон: светлый (`bg-white-90`)
- Большие вертикальные отступы (`py-16 lg:py-32 xl:py-40`)
- Горизонтальные отступы (`px-4 sm:px-6 lg:px-8`)

---

### 2. Секция с темным фоном (Benefits)

```vue
<BaseContainer 
  :as="'section'"
  :padding="true" 
  :vertical-padding="'sm'"
  :max-width="'full'"
  :bg="'bg-black-90'"
>
  <!-- Контент Benefits -->
</BaseContainer>
```

**Результат:**
- Рендерится как `<section>`
- Фон: темный (`bg-black-90`)
- Маленькие вертикальные отступы (`py-8 lg:py-16 xl:py-20`)

---

### 3. Контейнер без фона

```vue
<BaseContainer 
  :padding="true"
  :max-width="'xl'"
>
  <!-- Контент без фона -->
</BaseContainer>
```

**Результат:**
- Рендерится как `<div>`
- Без фона
- Горизонтальные отступы
- Максимальная ширина `max-w-screen-xl`

---

### 4. Hero секция

```vue
<BaseContainer 
  :as="'section'"
  :vertical-padding="'xl'"
  :bg="'bg-gradient-to-r from-primary to-primary-hover'"
>
  <h1>Hero Title</h1>
</BaseContainer>
```

**Результат:**
- Очень большие отступы (`py-20 lg:py-40 xl:py-48`)
- Градиентный фон

---

### 5. Узкий контейнер для текста

```vue
<BaseContainer 
  :padding="true"
  :max-width="'md'"
>
  <article>
    <p>Long text content...</p>
  </article>
</BaseContainer>
```

**Результат:**
- Центрированный контейнер
- Максимальная ширина `max-w-screen-md` (768px)

---

## 🔧 Использование в секциях

### До (дублирование кода):

```vue
<!-- FaqSection.vue -->
<section class="bg-white-90 w-full py-16 lg:py-32 xl:py-40">
  <div class="w-full px-4 sm:px-6 lg:px-8">
    <!-- Content -->
  </div>
</section>

<!-- BenefitsSection.vue -->
<section class="bg-black-90 w-full py-16">
  <div class="w-full px-4 sm:px-6 lg:px-8">
    <!-- Content -->
  </div>
</section>
```

❌ Проблемы:
- Дублирование классов
- Сложно менять отступы глобально
- Фон прописан в каждой секции

---

### После (централизовано):

```vue
<!-- FaqSection.vue -->
<BaseContainer 
  :as="'section'"
  :vertical-padding="true"
  :bg="'bg-white-90'"
>
  <!-- Content -->
</BaseContainer>

<!-- BenefitsSection.vue -->
<BaseContainer 
  :as="'section'"
  :vertical-padding="'sm'"
  :bg="'bg-black-90'"
>
  <!-- Content -->
</BaseContainer>
```

✅ Преимущества:
- Единая точка контроля отступов
- Легко менять систему отступов
- Меньше кода в секциях
- Семантическая разметка

---

## 🎯 Best Practices

### 1. Используйте `as="section"` для секций страницы

```vue
<!-- ✅ Правильно -->
<BaseContainer :as="'section'" :bg="'bg-white-90'">
  <h2>Section Title</h2>
</BaseContainer>

<!-- ❌ Неправильно -->
<section>
  <BaseContainer :bg="'bg-white-90'">
    <h2>Section Title</h2>
  </BaseContainer>
</section>
```

---

### 2. Группируйте похожие размеры

```vue
<!-- Hero, основные секции -->
<BaseContainer :vertical-padding="true" />

<!-- Вторичные секции -->
<BaseContainer :vertical-padding="'md'" />

<!-- Компактные блоки -->
<BaseContainer :vertical-padding="'sm'" />
```

---

### 3. Используйте `max-width` для узкого контента

```vue
<!-- Широкий контент (карточки, grid) -->
<BaseContainer :max-width="'full'" />

<!-- Текстовый контент -->
<BaseContainer :max-width="'lg'" />

<!-- Формы, узкие блоки -->
<BaseContainer :max-width="'md'" />
```

---

### 4. Комбинируйте с внутренним max-width

```vue
<BaseContainer 
  :as="'section'"
  :vertical-padding="true"
  :max-width="'full'"
>
  <!-- Ограничиваем ширину контента внутри -->
  <div class="max-w-[1048px] mx-auto">
    <h2>Centered Content</h2>
  </div>
</BaseContainer>
```

---

## 📱 Адаптивность

### Горизонтальные отступы (padding):
```css
px-4        /* Mobile: 16px */
sm:px-6     /* Tablet: 24px */
lg:px-8     /* Desktop: 32px */
```

### Вертикальные отступы (verticalPadding):
```css
/* sm */
py-8 lg:py-16 xl:py-20   /* 32px → 64px → 80px */

/* md */
py-12 lg:py-24 xl:py-32  /* 48px → 96px → 128px */

/* lg / true */
py-16 lg:py-32 xl:py-40  /* 64px → 128px → 160px */

/* xl */
py-20 lg:py-40 xl:py-48  /* 80px → 160px → 192px */
```

---

## 🔮 Планы по развитию

- [ ] Добавить prop `centered` для вертикального центрирования
- [ ] Добавить prop `fullHeight` для `min-h-screen`
- [ ] Поддержка кастомных классов через `class` prop
- [ ] Preset'ы для часто используемых конфигураций

---

## 🔗 Связанные компоненты

- [BaseHeading](./README.md#baseheading) - заголовки
- [BaseText](./README.md#basetext) - текстовые блоки
- [BaseButton](./BUTTONS.md) - кнопки

---

✅ **BaseContainer готов к использованию!**

