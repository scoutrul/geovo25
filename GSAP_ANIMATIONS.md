# 🎬 GSAP Анимации

## 📊 Обзор

В проекте используется **GSAP (GreenSock Animation Platform)** для профессиональных, плавных анимаций.

---

## ⚙️ Установка

```bash
pnpm add gsap
```

**Версия:** 3.13.0

---

## 🎯 Где используется

### AccordionItem (FAQ)

**Файл:** `src/components/ui/AccordionItem.vue`

**Анимации:**
- Плавное открытие/закрытие аккордиона
- Контроль height, opacity и display
- Разные easing для открытия и закрытия
- **Важно:** `display` меняется ПОСЛЕ завершения анимации закрытия

**Код:**
```javascript
import { gsap } from 'gsap'

// Открытие (более медленное и плавное)
gsap.set(element, { display: 'block' })  // Сначала показываем
gsap.to(element, {
  height: contentHeight,
  opacity: 1,
  duration: 0.4,        // 400ms
  ease: 'power2.out'    // Плавное замедление
})

// Закрытие (быстрее)
gsap.to(element, {
  height: 0,
  opacity: 0,
  duration: 0.3,        // 300ms
  ease: 'power2.in',    // Плавное ускорение
  onComplete: () => {
    // После завершения анимации скрываем элемент
    gsap.set(element, { display: 'none' })
  }
})

// Инициализация (мгновенная)
gsap.set(element, {
  display: 'block',
  height: contentHeight,
  opacity: 1
})
```

---

## 🎨 Easing функции

### power2.out
- Используется для **открытия**
- Быстрый старт, плавное замедление
- Создает ощущение "выпадения"

### power2.in
- Используется для **закрытия**
- Плавный старт, быстрое завершение
- Создает ощущение "схлопывания"

### Другие доступные easing:
```javascript
// Линейные
'none'              // Без easing
'linear'            // Линейная анимация

// Power
'power1.in'         // Слабое ускорение
'power1.out'        // Слабое замедление
'power1.inOut'      // Ускорение + замедление

'power2.in'         // Среднее ускорение
'power2.out'        // Среднее замедление ✅
'power2.inOut'      // Ускорение + замедление

'power3.in'         // Сильное ускорение
'power3.out'        // Сильное замедление
'power3.inOut'      // Ускорение + замедление

// Elastic (пружинистость)
'elastic.in'        // Пружина в начале
'elastic.out'       // Пружина в конце
'elastic.inOut'     // Двойная пружина

// Bounce (отскок)
'bounce.in'         // Отскок в начале
'bounce.out'        // Отскок в конце
'bounce.inOut'      // Двойной отскок

// Back (возврат)
'back.in'           // Возврат назад в начале
'back.out'          // Возврат назад в конце
'back.inOut'        // Двойной возврат

// Expo
'expo.in'           // Экспоненциальное ускорение
'expo.out'          // Экспоненциальное замедление
'expo.inOut'        // Экспоненциальное ускорение + замедление
```

---

## 💡 Примеры использования

### Базовая анимация

```vue
<script setup>
import { ref, onMounted } from 'vue'
import { gsap } from 'gsap'

const elementRef = ref(null)

onMounted(() => {
  gsap.from(elementRef.value, {
    opacity: 0,
    y: 50,
    duration: 0.6,
    ease: 'power2.out'
  })
})
</script>

<template>
  <div ref="elementRef">
    Animated content
  </div>
</template>
```

### Последовательная анимация

```javascript
import { gsap } from 'gsap'

// Timeline для последовательных анимаций
const tl = gsap.timeline()

tl.from('.title', {
  opacity: 0,
  y: -50,
  duration: 0.5
})
.from('.content', {
  opacity: 0,
  y: 30,
  duration: 0.5
}, '-=0.3')  // Начать на 0.3s раньше
.from('.button', {
  scale: 0,
  duration: 0.3
})
```

### Анимация при скролле

```javascript
import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

gsap.from('.section', {
  scrollTrigger: {
    trigger: '.section',
    start: 'top 80%',
    end: 'top 20%',
    scrub: true
  },
  opacity: 0,
  y: 100
})
```

### Hover анимация

```vue
<script setup>
import { ref } from 'vue'
import { gsap } from 'gsap'

const buttonRef = ref(null)

const onMouseEnter = () => {
  gsap.to(buttonRef.value, {
    scale: 1.05,
    duration: 0.2,
    ease: 'power2.out'
  })
}

const onMouseLeave = () => {
  gsap.to(buttonRef.value, {
    scale: 1,
    duration: 0.2,
    ease: 'power2.out'
  })
}
</script>

<template>
  <button
    ref="buttonRef"
    @mouseenter="onMouseEnter"
    @mouseleave="onMouseLeave"
  >
    Hover me
  </button>
</template>
```

### Stagger (каскадная анимация)

```javascript
import { gsap } from 'gsap'

// Анимировать элементы с задержкой
gsap.from('.card', {
  opacity: 0,
  y: 50,
  duration: 0.5,
  stagger: 0.1,  // 0.1s задержка между элементами
  ease: 'power2.out'
})
```

---

## 🚀 Best Practices

### 1. Используйте gsap.set() для начальных состояний
```javascript
// ✅ Правильно
gsap.set(element, { opacity: 0, y: 50 })
gsap.to(element, { opacity: 1, y: 0, duration: 0.5 })

// ❌ Неправильно (лишняя анимация)
gsap.to(element, { opacity: 1, y: 0, duration: 0 })
```

### 2. Используйте Timeline для сложных анимаций
```javascript
// ✅ Правильно
const tl = gsap.timeline()
tl.to(el1, { x: 100 })
  .to(el2, { y: 100 })
  .to(el3, { scale: 1.5 })

// ❌ Неправильно (сложно контролировать)
gsap.to(el1, { x: 100, delay: 0 })
gsap.to(el2, { y: 100, delay: 0.5 })
gsap.to(el3, { scale: 1.5, delay: 1 })
```

### 3. Очищайте анимации при unmount
```javascript
import { onBeforeUnmount } from 'vue'

const animation = gsap.to(...)

onBeforeUnmount(() => {
  animation.kill()
})
```

### 4. Используйте refs для элементов
```javascript
// ✅ Правильно
const elementRef = ref(null)
gsap.to(elementRef.value, {...})

// ❌ Неправильно (может не работать)
gsap.to('.selector', {...})
```

### 5. Управляйте display правильно при анимациях
```javascript
// ✅ Правильно - display меняется ПОСЛЕ анимации
gsap.to(element, {
  opacity: 0,
  onComplete: () => {
    gsap.set(element, { display: 'none' })
  }
})

// ❌ Неправильно - v-show прервет анимацию
<div v-show="isOpen">  // Скроется мгновенно!
  Animated content
</div>

// ✅ Правильно - GSAP управляет всем
<div ref="el" style="display: none">
  Animated content
</div>
```

---

## ⚡ Производительность

### Оптимизированные свойства
✅ **Быстрые** (GPU ускорение):
- `x`, `y`, `rotation`, `scale`
- `opacity`
- `transform`

⚠️ **Медленные** (CPU):
- `width`, `height` (но используются для аккордиона)
- `top`, `left`
- `margin`, `padding`

### Советы по производительности
1. Используйте `will-change` для сложных анимаций
2. Избегайте анимации слишком многих элементов одновременно
3. Используйте `force3D: true` для GPU ускорения

---

## 🎯 Когда использовать GSAP vs CSS

### Используйте GSAP когда:
✅ Нужна сложная последовательная анимация  
✅ Нужен точный контроль timing  
✅ Анимация зависит от динамических значений (как scrollHeight)  
✅ Нужны advanced easing функции  
✅ Нужна анимация при скролле  

### Используйте CSS когда:
✅ Простые hover эффекты  
✅ Transitions между двумя состояниями  
✅ Когда производительность критична  
✅ Простые fade in/out  

---

## 📚 Документация

- [GSAP Docs](https://greensock.com/docs/)
- [GSAP Ease Visualizer](https://greensock.com/ease-visualizer/)
- [GSAP Vue Integration](https://greensock.com/vue/)

---

## 🔮 Дальнейшее использование

### Планы по использованию GSAP:
- [ ] Анимация появления секций при скролле
- [ ] Плавные переходы между страницами
- [ ] Анимированные графики и charts
- [ ] Микро-анимации для кнопок
- [ ] Параллакс эффекты
- [ ] Морфинг SVG
- [ ] Анимация при загрузке страницы

---

✅ **GSAP готов к использованию в проекте!**

